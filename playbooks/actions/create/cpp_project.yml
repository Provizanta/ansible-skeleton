---

- hosts: localhost
  roles:
    - role: user/dev/create/empty_project/cpp
      vars:
        path: 
          ~/test/
        project:
          name: "my First Project"
          description: "cpp project description"
          version: 0.5
          author:
            name: Dezider Lakatos
            email: test@test.com
          license: WTFPL
        build: cmake
        test:
          integration: true
          unittest: Catch2
        scm: git
  become: no
  environment: "{{ proxy_env if proxy_env is defined else '' }}"
